# example
# [docker:container-name]
# image = <use image> or
# dockerfile = <build from dockerfile>
#

requires = ['tox-uv', 'tox-docker', 'pytest']
env-list = [
  # "py310_alpine",
  # "py310_ubuntu",
  # "py310_debian",
  # "py310_rocky",
  # "py310_alpine_user",
  "py310_ubuntu_user",
  # "py310_debian_user",
  # "py310_rocky_user"
]

[env.py310_ubuntu]

docker = "ubuntu"
chdir = "/usr/src/noobs/"
commands = pytest


[docker:ubuntu]
image = "docker.io/library/ubuntu:22.04"
volumes = [
  # source code
  "bind:ro:/home/krystofair/worklab/noobs/:/usr/src/noobs/",
]

#
# [docker:debian]
# image = "docker.io/library/debian:12"
#
#
# [docker:rocky]
# image = "docker.io/rockylinux/rockylinux:8"


